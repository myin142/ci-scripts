.setup_nx_args: &setup_nx_args
  - if [ ! -z $CI_EXTERNAL_PULL_REQUEST_IID ]; then NX_BASE_ARGS="--base $CI_EXTERNAL_PULL_REQUEST_TARGET_BRANCH_SHA --head $$CI_EXTERNAL_PULL_REQUEST_SOURCE_BRANCH_SHA"
  - if [ -z $CI_EXTERNAL_PULL_REQUEST_IID ]; then NX_BASE_ARGS="--base $CI_COMMIT_BEFORE_SHA --head $CI_COMMIT_SHA"
